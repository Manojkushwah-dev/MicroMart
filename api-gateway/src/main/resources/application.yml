server:
  port: 8081

spring:
  application:
    name: api-gateway
  datasource:
    url: jdbc:mysql://localhost:3306/gatewaydb
    username: root
    password: Manoj@8719895574
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
